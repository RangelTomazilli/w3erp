import styled from "styled-components";
import { colorsTheme } from "../../../../../theme";

export const StyledTitle = styled.h4`
  font-weight: 400;
  font-size: 16px;
  color: ${colorsTheme.azul4};
`;
